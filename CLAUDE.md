# p-o-de-mel

Project context for Claude Code sessions in this repository.

## Status

The repository is currently a blank slate — no application code, build
tooling, or dependencies have been added yet. Sections below are stubs to fill
in as the project takes shape.

## Stack

_Not chosen yet._ Record the language, framework, and package manager here once
decided, so sessions don't have to infer them.

## Layout

```
.
├── CLAUDE.md   # this file
├── README.md
└── .claude/
    └── settings.json   # project-wide Claude Code settings (committed)
```

## Commands

_No build, test, or lint commands exist yet._ Add them here as they are
introduced — for example:

- Install: _TBD_
- Test: _TBD_
- Lint: _TBD_

## Conventions

- Default branch is `main`; work happens on feature branches.
- Personal, uncommitted Claude Code overrides belong in
  `.claude/settings.local.json`, which is gitignored. Team-wide settings go in
  `.claude/settings.json`.
